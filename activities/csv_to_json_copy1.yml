version: 1
job:
  type: "ingestion"
  description: "Special desc"
source:
  endpoint: "lo_hive"
  properties:
    table: "users"
    format_properties:
      clean_column_name: false
schema:
  schema_derivation_strategy: "source"
  strict_schema_check: true
  discard: true
  columns: []
target:
  endpoint: "lo_dbfs"
  properties:
    format: "json"
    charset: "UTF-8"
    path: "json_tgt/users1/"
    preserve_hierarchy: false
    format_properties:
      merge_file: true
